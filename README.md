# motion-synth

This application utilizes the device orientation API to collect the acceleration data from the phone and broadcast them to the browser using WebSockets. The streaming data is then visualized in WebGL and mapped to two filters to perform real-time sound processing based on the notes generated by a step sequencer.
